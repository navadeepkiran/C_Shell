diff --git a/Makefile b/Makefile
index 2d5f9e4..d9ac913 100644
--- a/Makefile
+++ b/Makefile
@@ -22,6 +22,7 @@ OBJS = \
   $K/fs.o \
   $K/log.o \
   $K/sleeplock.o \
+  $K/readcount.o \
   $K/file.o \
   $K/pipe.o \
   $K/exec.o \
@@ -73,6 +74,14 @@ CFLAGS += -fno-builtin-printf -fno-builtin-fprintf -fno-builtin-vprintf
 CFLAGS += -I.
 CFLAGS += $(shell $(CC) -fno-stack-protector -E -x c /dev/null >/dev/null 2>&1 && echo -fno-stack-protector)
 
+ifdef SCHEDULER
+ifeq ($(SCHEDULER), FCFS)
+    CFLAGS += -DFCFS
+endif
+ifeq ($(SCHEDULER), CFS)
+    CFLAGS += -DCFS
+endif
+endif
 # Disable PIE when possible (for Ubuntu 16.10 toolchain)
 ifneq ($(shell $(CC) -dumpspecs 2>/dev/null | grep -e '[^f]no-pie'),)
 CFLAGS += -fno-pie -no-pie
@@ -141,6 +150,9 @@ UPROGS=\
 	$U/_zombie\
 	$U/_logstress\
 	$U/_forphan\
+	$U/_readcount\
+	$U/_cfs_test\
+	$U/_test_cfs\
 	$U/_dorphan\
 
 fs.img: mkfs/mkfs README $(UPROGS)
diff --git a/kernel/main.c b/kernel/main.c
index f0d3171..a17254a 100644
--- a/kernel/main.c
+++ b/kernel/main.c
@@ -3,6 +3,7 @@
 #include "memlayout.h"
 #include "riscv.h"
 #include "defs.h"
+#include "readcount.h"
 
 volatile static int started = 0;
 
@@ -30,6 +31,7 @@ main()
     virtio_disk_init(); // emulated hard disk
     userinit();      // first user process
     __sync_synchronize();
+    readcountinit();
     started = 1;
   } else {
     while(started == 0)
diff --git a/kernel/proc.c b/kernel/proc.c
index 9d6cf3f..da96a25 100644
--- a/kernel/proc.c
+++ b/kernel/proc.c
@@ -29,6 +29,26 @@ struct spinlock wait_lock;
 // Allocate a page for each process's kernel stack.
 // Map it high in memory, followed by an invalid
 // guard page.
+
+// my edit
+// ADD THIS ARRAY
+#ifdef CFS
+// nice values from -20 to 19 map to weights
+// calculated as 1024 / (1.25^nice)
+static const int weights[40] = {
+  88761, 71755, 56364, 46273, 36291, // nice -20 to -16
+  29154, 23254, 18705, 14949, 11916, // nice -15 to -11
+  9548,  7620,  6100,  4904,  3906,  // nice -10 to -6
+  3121,  2501,  1991,  1586,  1277,  // nice -5 to -1
+  1024,  820,   655,   526,   423,   // nice 0 to 4
+  335,   272,   215,   172,   137,   // nice 5 to 9
+  110,   87,    70,    56,    45,    // nice 10 to 14
+  36,    29,    23,    18,    15     // nice 15 to 19
+};
+#endif
+
+
+
 void
 proc_mapstacks(pagetable_t kpgtbl)
 {
@@ -124,7 +144,17 @@ allocproc(void)
 found:
   p->pid = allocpid();
   p->state = USED;
-
+  // my edit
+  #if defined(FCFS) || defined(CFS)
+  acquire(&tickslock);
+  p->creation_time = ticks;
+  release(&tickslock);
+  #endif
+
+  #ifdef CFS
+  p->nice = NICE_DEFAULT; // Default nice value
+  p->vruntime = 0;      // Initialize vruntime
+  #endif
   // Allocate a trapframe page.
   if((p->trapframe = (struct trapframe *)kalloc()) == 0){
     freeproc(p);
@@ -418,23 +448,105 @@ kwait(uint64 addr)
 //  - swtch to start running that process.
 //  - eventually that process transfers control
 //    via swtch back to the scheduler.
+// scheduler()
+// ...
+// It is simpler to replace the whole function.
+//my edit
 void
 scheduler(void)
 {
   struct proc *p;
   struct cpu *c = mycpu();
-
+  
   c->proc = 0;
   for(;;){
-    // The most recent process to run may have had interrupts
-    // turned off; enable them to avoid a deadlock if all
-    // processes are waiting. Then turn them back off
-    // to avoid a possible race between an interrupt
-    // and wfi.
+    // Avoid deadlock by ensuring that devices can interrupt.
     intr_on();
-    intr_off();
 
-    int found = 0;
+#if defined(FCFS)
+    // --- FCFS SCHEDULER ---
+    struct proc *earliest_proc = 0;
+
+    // Find the runnable process with the earliest creation time.
+    for(p = proc; p < &proc[NPROC]; p++) {
+      acquire(&p->lock);
+      if(p->state == RUNNABLE) {
+        if(earliest_proc == 0 || p->creation_time < earliest_proc->creation_time) {
+          if(earliest_proc)
+            release(&earliest_proc->lock);
+          earliest_proc = p;
+          continue; // keep lock on earliest_proc
+        }
+      }
+      release(&p->lock);
+    }
+
+    if(earliest_proc) {
+      // Switch to the chosen process.
+      p = earliest_proc;
+      p->state = RUNNING;
+      c->proc = p;
+      swtch(&c->context, &p->context);
+      // Process is done running for now.
+      c->proc = 0;
+      release(&p->lock);
+    }
+
+#elif defined(CFS)
+    // --- COMPLETELY FAIR SCHEDULER ---
+    struct proc *min_vruntime_proc = 0;
+    int num_runnable = 0;
+
+    // Count runnable processes
+    for (p = proc; p < &proc[NPROC]; p++) {
+        if (p->state == RUNNABLE) {
+            num_runnable++;
+        }
+    }
+
+    // Find the runnable process with the minimum vruntime.
+    for (p = proc; p < &proc[NPROC]; p++) {
+      acquire(&p->lock);
+      if (p->state == RUNNABLE) {
+        if (min_vruntime_proc == 0 || p->vruntime < min_vruntime_proc->vruntime) {
+          if (min_vruntime_proc)
+            release(&min_vruntime_proc->lock);
+          min_vruntime_proc = p;
+          continue;
+        }
+      }
+      release(&p->lock);
+    }
+    
+    if (min_vruntime_proc) {
+      p = min_vruntime_proc;
+      
+      // Calculate time slice
+      uint time_slice = 48 / num_runnable;
+      if (time_slice < 3) {
+          time_slice = 3;
+      }
+
+      // Run the process for its time slice
+      p->state = RUNNING;
+      c->proc = p;
+
+      // This is a simplified way to handle the time slice.
+      // The process will run until the next timer interrupt,
+      // where its vruntime will be updated. The loop here
+      // simulates running for the whole slice at once for vruntime update.
+      int weight = weights[p->nice + 20];
+      p->vruntime += (time_slice * 1024) / weight;
+      
+      swtch(&c->context, &p->context);
+
+      // Process is done running for now.
+      c->proc = 0;
+      release(&p->lock);
+    }
+
+#else
+    // --- DEFAULT ROUND-ROBIN SCHEDULER ---
     for(p = proc; p < &proc[NPROC]; p++) {
       acquire(&p->lock);
       if(p->state == RUNNABLE) {
@@ -448,14 +560,10 @@ scheduler(void)
         // Process is done running for now.
         // It should have changed its p->state before coming back.
         c->proc = 0;
-        found = 1;
       }
       release(&p->lock);
     }
-    if(found == 0) {
-      // nothing to run; stop running on this core until an interrupt.
-      asm volatile("wfi");
-    }
+#endif
   }
 }
 
@@ -570,12 +678,34 @@ sleep(void *chan, struct spinlock *lk)
 void
 wakeup(void *chan)
 {
+  //my edit
+  #ifdef CFS
+  // Find the minimum vruntime among all runnable processes.
+  uint64 min_vruntime = -1;
+  struct proc *p_temp;
+  for (p_temp = proc; p_temp < &proc[NPROC]; p_temp++) {
+    if (p_temp->state == RUNNABLE) {
+      if (min_vruntime == -1 || p_temp->vruntime < min_vruntime) {
+        min_vruntime = p_temp->vruntime;
+      }
+    }
+  }
+  #endif
+
+
   struct proc *p;
 
   for(p = proc; p < &proc[NPROC]; p++) {
     if(p != myproc()){
       acquire(&p->lock);
       if(p->state == SLEEPING && p->chan == chan) {
+        // my edit
+        #ifdef CFS
+        // Set vruntime for the waking process
+        if (min_vruntime != -1) {
+          p->vruntime = min_vruntime;
+        }
+        #endif
         p->state = RUNNABLE;
       }
       release(&p->lock);
@@ -682,6 +812,11 @@ procdump(void)
     else
       state = "???";
     printf("%d %s %s", p->pid, state, p->name);
+    #if defined(FCFS)
+    printf(" (creation_time: %ld)", p->creation_time);
+    #elif defined(CFS)
+    printf(" (nice: %d, vruntime: %ld)", p->nice, p->vruntime);
+    #endif
     printf("\n");
   }
 }
diff --git a/kernel/proc.h b/kernel/proc.h
index d021857..218e218 100644
--- a/kernel/proc.h
+++ b/kernel/proc.h
@@ -1,4 +1,8 @@
 // Saved registers for kernel context switches.
+//my edit
+#define NICE_DEFAULT 0
+#define NICE_MIN -20
+#define NICE_MAX 19
 struct context {
   uint64 ra;
   uint64 sp;
@@ -104,4 +108,13 @@ struct proc {
   struct file *ofile[NOFILE];  // Open files
   struct inode *cwd;           // Current directory
   char name[16];               // Process name (debugging)
+  //my edit
+    #if defined(FCFS) || defined(CFS)
+  uint64 creation_time;      // For FCFS: time process was created
+  #endif
+  
+  #ifdef CFS
+  int nice;                  // For CFS: nice value for priority
+  uint64 vruntime;             // For CFS: virtual runtime
+  #endif
 };
diff --git a/kernel/syscall.c b/kernel/syscall.c
index 076d965..b694c91 100644
--- a/kernel/syscall.c
+++ b/kernel/syscall.c
@@ -101,6 +101,7 @@ extern uint64 sys_unlink(void);
 extern uint64 sys_link(void);
 extern uint64 sys_mkdir(void);
 extern uint64 sys_close(void);
+extern uint64 sys_getreadcount(void);
 
 // An array mapping syscall numbers from syscall.h
 // to the function that handles the system call.
@@ -126,6 +127,7 @@ static uint64 (*syscalls[])(void) = {
 [SYS_link]    sys_link,
 [SYS_mkdir]   sys_mkdir,
 [SYS_close]   sys_close,
+[SYS_getreadcount] sys_getreadcount,
 };
 
 void
diff --git a/kernel/syscall.h b/kernel/syscall.h
index 3dd926d..7e45e55 100644
--- a/kernel/syscall.h
+++ b/kernel/syscall.h
@@ -20,3 +20,4 @@
 #define SYS_link   19
 #define SYS_mkdir  20
 #define SYS_close  21
+#define SYS_getreadcount 22
\ No newline at end of file
diff --git a/kernel/sysfile.c b/kernel/sysfile.c
index d8234ce..63a19d5 100644
--- a/kernel/sysfile.c
+++ b/kernel/sysfile.c
@@ -15,6 +15,7 @@
 #include "sleeplock.h"
 #include "file.h"
 #include "fcntl.h"
+#include "readcount.h"
 
 // Fetch the nth word-sized system call argument as a file descriptor
 // and return both the descriptor and the corresponding struct file.
@@ -65,6 +66,8 @@ sys_dup(void)
   return fd;
 }
 
+
+
 uint64
 sys_read(void)
 {
@@ -76,7 +79,14 @@ sys_read(void)
   argint(2, &n);
   if(argfd(0, 0, &f) < 0)
     return -1;
-  return fileread(f, p, n);
+  
+  uint64 result = fileread(f, p, n);
+  
+  if(result > 0) {
+    incrementreadcount(result);
+  }
+  
+  return result;
 }
 
 uint64
diff --git a/kernel/sysproc.c b/kernel/sysproc.c
index 3044d00..856c7e5 100644
--- a/kernel/sysproc.c
+++ b/kernel/sysproc.c
@@ -6,6 +6,13 @@
 #include "spinlock.h"
 #include "proc.h"
 #include "vm.h"
+#include "readcount.h"
+
+uint64
+sys_getreadcount(void)
+{
+  return getreadcount();
+}
 
 uint64
 sys_exit(void)
diff --git a/user/user.h b/user/user.h
index ac84de9..51e50b7 100644
--- a/user/user.h
+++ b/user/user.h
@@ -1,5 +1,5 @@
 #define SBRK_ERROR ((char *)-1)
-
+typedef unsigned uint;
 struct stat;
 
 // system calls
@@ -24,6 +24,7 @@ int getpid(void);
 char* sys_sbrk(int,int);
 int pause(int);
 int uptime(void);
+int getreadcount(void);
 
 // ulib.c
 int stat(const char*, struct stat*);
diff --git a/user/usys.pl b/user/usys.pl
index c5d4c3a..05223bf 100755
--- a/user/usys.pl
+++ b/user/usys.pl
@@ -42,3 +42,4 @@ entry("getpid");
 entry("sbrk");
 entry("pause");
 entry("uptime");
+entry("getreadcount");
